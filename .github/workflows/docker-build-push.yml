- name: Build and Push
  uses: docker/build-push-action@v5
  with:
    context: . # 显式指定上下文为根目录
    platforms: linux/amd64,linux/arm64
    push: true
    tags: ${{ steps.meta.outputs.tags }}
    no-cache: true # 禁用缓存，避免旧缓存干扰
    cache-from: type=gha
    cache-to: type=gha,mode=max
